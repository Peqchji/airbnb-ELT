snapshots:
  - name: dim_hosts
    relation: ref('ephemeral_hosts')
    config:
      schema: mart
      database: AIRBNB_ELT
      unique_key: HOST_ID
      strategy: timestamp
      updated_at: HOST_CREATED_AT
      dbt_valid_to_current: NULL