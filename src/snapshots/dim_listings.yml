snapshots:
  - name: dim_listings
    relation: ref('ephemeral_listings')
    config:
      schema: mart
      database: AIRBNB_ELT
      unique_key: LISTING_ID
      strategy: timestamp
      updated_at: LISTING_CREATED_AT
      dbt_valid_to_current: NULL